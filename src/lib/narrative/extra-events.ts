import type { GameEvent } from "@/types/career";

/**
 * Eventos narrativos adicionales para la carrera principal.
 * Por ahora está vacío - se irá expandiendo con más eventos según sea necesario.
 */
export const EXTRA_EVENTS: GameEvent[] = [];
